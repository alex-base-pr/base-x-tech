# QA baseline — unchanged `live` site, 2026-09-24

`npm run qa` on `upstream/live@16c2853`, before any change. Chromium. 117 checks: 56 pass, 61 fail.
Every failure below is a target for the quick EN release; the REQ that fixes it is in brackets.

| Area | Result on live | Fix |
|------|----------------|-----|
| HTTP 200 on all 15 EN URLs | pass | — |
| One `<h1>` per page, titles ≤ 60, unique | pass | — |
| Meta description ≤ 160 | fail on `seo-promotion` (169) | page goes noindex (REQ-008) |
| **Canonical in static HTML** | fail on all 15 — canonical is injected by JS (`layout/head-alt.ejs:26`), invisible to non-JS crawlers | REQ-005 |
| **Console errors** | every page: 404 for `/images/sections/bg-mob.jpg`, preloaded with `fetchpriority=high` from `head.ejs:7` / `head-alt.ejs:35` (real file is `/images/sections/hero/bg-mob.jpg`, home only) | remove/fix preload |
| Horizontal scroll 375/768/1440 | pass (off-canvas drawer is clipped) | — |
| **Services menu** | 10 service links incl. absorbed pages; must be the 6 | REQ-007 |
| **sitemap.xml** | 14 EN URLs incl. absorbed/ppc/seo; URLs without trailing slash → every entry 301s | REQ-009 |
| Internal links (linkinator, 358) | 14 broken = the `bg-mob.jpg` preload | as above |
| **Accessibility (axe, critical/serious)** | fails on every page: `button-name` (icon buttons without label), `link-name` (icon links), `color-contrast`, `target-size`; `image-alt` on 2 pages | NFR-002 — fix labels/alts in shared header/footer/drawer; contrast per page |
| **robots.txt** | AI bots allowed except training signal: `Content-Signal: ai-train=no` | REQ-040 |
| Copy in raw HTML inside `<main>` | pass (> 150 words, h1 in main) | — |
| **JSON-LD** | none on any page | REQ-044 |
| **llms.txt** | lists 3 URLs incl. headless (absorbed); no `llms-full.txt` | REQ-042/043 |

Not measured yet: Lighthouse (NFR-001), WebKit run (`--project=webkit`, tagged `@cross`).

## After the site-wide pass (same day)

115 / 117 pass. Remaining 2 = `FAQPage` JSON-LD on Shopify Development and Web Design, which arrives with Marko's FAQ copy.
Known and accepted: several nested `<main>` elements inside legacy sections (reviews, works, team, approach, FAQ) —
styles depend on them; the redesign replaces these sections.
